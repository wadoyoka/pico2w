/**
 * AWS IoT MQTT クライアント 設定ファイル サンプル
 *
 * 使用方法:
 * 1. このファイルを "config.h" としてコピーします
 * 2. 各設定値を実際の値に置き換えます
 * 3. 実際の config.h ファイルは .gitignore に追加して、公開リポジトリにアップロードしないようにします
 */

#ifndef CONFIG_H
#define CONFIG_H

//=============================================================================
// WiFi 設定
//=============================================================================
const char *WIFI_SSID = "あなたのWiFi-SSID";           // WiFi の SSID
const char *WIFI_PASSWORD = "あなたのWiFi-パスワード"; // WiFi のパスワード

//=============================================================================
// AWS IoT Core 設定
//=============================================================================
// AWS IoT Core のエンドポイント
// AWS IoT コンソールの「設定」から確認できます
// 形式: xxxxxxx-ats.iot.リージョン.amazonaws.com
const char *AWS_IOT_ENDPOINT = "xxxxxxx-ats.iot.ap-northeast-1.amazonaws.com";

// クライアント ID（デバイス名）
// AWS IoT に登録したモノの名前、または一意の識別子
const char *CLIENT_ID = "arduino_device";

//=============================================================================
// MQTT トピック設定
//=============================================================================
// デバイスからデータを送信するトピック
const char *PUBLISH_TOPIC = "device/data";

// デバイスがコマンドを受信するトピック
const char *SUBSCRIBE_TOPIC = "device/command";

//=============================================================================
// タイミング設定
//=============================================================================
// データ送信間隔（ミリ秒）
const unsigned long PUBLISH_INTERVAL = 5000; // 5秒ごとに送信

// 再接続間隔（ミリ秒）
const unsigned long RECONNECT_INTERVAL = 5000; // 5秒ごとに再接続を試行

//=============================================================================
// デバッグ設定
//=============================================================================
// デバッグモード（詳細なログ出力）
const bool DEBUG_MODE = true;

#endif // CONFIG_H